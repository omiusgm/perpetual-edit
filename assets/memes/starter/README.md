# Procedural starter pack

These PNG files are deterministic output from
`tools/generate_starter_assets.py`. They use only gradients and geometric
OpenCV primitives. Run the script after changing the generator to rebuild all
cards.
